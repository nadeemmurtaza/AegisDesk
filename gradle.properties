org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
# Room KMP 2.7.0-alpha13 breaks under KSP2's auto-created kspCommonMainKotlinMetadata
# task (MissingType — see the tracked build_debug.log and docs/rules/compatibility.md).
# Pin KSP1 explicitly; add kspCommonMainMetadata to a module only if it is ever needed.
ksp.useKSP2=false

